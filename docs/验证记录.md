# Agent 验证记录

本轮在原项目上接入了文章助手，并保留直接访问 MySQL 的设计。

- 后端：`uv run --locked python -m pytest -q`，14 项通过。
- Agent 测试运行真实 LangChain create_agent 工具循环，使用可控假模型和临时 SQLite，检查增删改查、校验错误、并发工具、部分成功、异常回滚及登录保护。
- 实际运行服务：`/api/agent/status` 正常返回，现有 MySQL 分类接口正常读取。
- 前端：`npm run build` 通过；依赖审计 0 漏洞。
- 浏览器：1280×900 和 390×844，检查未登录/缺配置提示、示例填入、等待时禁止重复发送、工具执行记录展开、文章链接、401 后提示及输入恢复、清空对话。
- 页面成功回复使用模拟 HTTP 响应验证显示；401 检查使用真实后端。未用测试对话修改现有 MySQL 文章。
- 尚未验证真实模型调用：当前 LLM_API_KEY 未配置。

模型配置与中文入门教程见 README.md 和 Agent入门.md。
